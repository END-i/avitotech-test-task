import React from "react";
import styled from "styled-components";

const Title = styled.div`
  font-size: 36px;
  line-height: 34px;
  text-align: center;
  text-transform: uppercase;
  margin: 15px 0;
`;

export default function () {
  return <Title>Test APP</Title>;
}
